# Thee You Space Backend — Environment Variable Reference
# Copy this to .env and fill in real values before running

# ─────────────────────────────────────────────
# SERVER
# ─────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ─────────────────────────────────────────────
# CORS — Frontend origin allowed
# In production: set to your real domain
# ─────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173

# ─────────────────────────────────────────────
# GOOGLE SHEETS — Live Slot Management (MAKE ONCE, NEVER TOUCH AGAIN)
# The counsellor just edits the Google Sheet to update availability.
# Backend syncs automatically every 60 minutes.
#
# Setup (one time only):
#   1. Create a Google Sheet with columns: Date | Time
#      Example rows:
#       Monday Mar 3    | 10:00 AM
#       Monday Mar 3    | 2:00 PM
#   2. File → Share → "Anyone with the link" → Viewer
#   3. Copy the sheet URL (looks like: https://docs.google.com/spreadsheets/d/SHEET_ID/edit)
#   4. Change "edit" to "export?format=csv"
#   5. Paste below — DONE FOREVER
#
# To force a manual sync from the sheet right now:
#   curl -X POST http://localhost:3000/api/slots/sync -H "X-API-Key: YOUR_EXPORT_API_KEY"
# ─────────────────────────────────────────────
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID_HERE/export?format=csv

# ─────────────────────────────────────────────
# RAZORPAY
# Get your keys from: https://dashboard.razorpay.com/app/keys
# Use TEST keys during development (prefix: rzp_test_)
# Use LIVE keys only in production (prefix: rzp_live_)
# ─────────────────────────────────────────────
RAZORPAY_KEY_ID=rzp_test_REPLACE_WITH_YOUR_KEY
RAZORPAY_KEY_SECRET=REPLACE_WITH_YOUR_SECRET
RAZORPAY_WEBHOOK_SECRET=REPLACE_WITH_YOUR_WEBHOOK_SECRET

# ─────────────────────────────────────────────
# EMAIL (Gmail SMTP)
# Use an App Password, NOT your Gmail login password.
# How to get App Password:
#   Google Account → Security → 2-Step Verification → App passwords
#   Select "Mail" + device → Generate → Copy the 16-char password
# ─────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=theeyoucounsellingspace@gmail.com
SMTP_PASS=REPLACE_WITH_16_CHAR_APP_PASSWORD
SMTP_FROM=Thee You Space <theeyoucounsellingspace@gmail.com>

# ─────────────────────────────────────────────
# ADMIN API KEY
# Used for:
#   - GET  /api/export/bookings  — download booking CSV
#   - POST /api/slots/upload     — upload Excel availability
#   - GET  /api/slots/status     — view upload status
# Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ─────────────────────────────────────────────
EXPORT_API_KEY=REPLACE_WITH_SECURE_RANDOM_KEY
